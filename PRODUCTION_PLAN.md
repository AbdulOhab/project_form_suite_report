# Project Form Suite — Step-by-Step Completion & Production Guide

## Context

প্রোজেক্ট ৮০% শেষ। বাস্তবে অডিট করে দেখা গেছে: **server ~40-50% production-ready, client ~70-75% production-ready**। কোর ফিচার (auth, notice CRUD, answer submission, user management, data aggregation) কাজ করে — কিন্তু production-এ দেওয়ার আগে security, config, এবং কিছু stub/broken জায়গা ঠিক করতে হবে।

এই ডকুমেন্ট হলো একটা step-by-step roadmap — প্রতিটা ধাপ অনুসরণ করলে প্রোজেক্ট production-ready হয়ে যাবে।

> **আপডেট (২০২৬-০৮-০৪):** নিচের অনেক আইটেম ইতিমধ্যে ফিক্স হয়ে গেছে — Phase 1 (env/secrets), Phase 2 (auth middleware + RBAC), এবং Phase 4/6-এর বড় অংশ। প্রতিটা টেবিল রো-তে ✅/⬜ মার্ক করে বর্তমান অবস্থা দেখানো হলো; পুরনো roadmap স্ট্রাকচার রেফারেন্সের জন্য রাখা হয়েছে।

---

## যা এখন কাজ করে (Working)

**Backend:**
- JWT login (`POST /submit`), auth check (`GET /check-user`)
- Notice CRUD, Answer CRUD
- Branch/Zonal/Thana/Admin user CRUD
- Data aggregation (dataCheckController, sumsDataController) — complex but functional logic
- MongoDB connection via env (with fallback)

**Frontend:**
- 52 routes wired up across public + dashboard (`BrowserRouter`, not `HashRouter`)
- Login flow + token storage
- Notice board (search, pagination, filtering)
- Notice create/edit, Q&A submission
- User CRUD pages for all roles
- Bangla localization + date/number formatting
- Role-based UI rendering

---

## Critical Issues Found (must-fix before prod)

### 🔴 Security Blockers
| # | File | Issue |
|---|------|-------|
| 1 | ✅ **ফিক্স হয়ে গেছে** — `authController.js`/`authMiddleware.js` এখন `process.env.JWT_SECRET` ব্যবহার করে | ~~Hardcoded JWT secret `"3e9b2825-..."`~~ |
| 2 | ✅ **ফিক্স হয়ে গেছে** — [server-side/index.js](server-side/index.js) এখন `origin: process.env.CORS_ORIGIN \|\| "http://localhost:3000"` | ~~`CORS origin: "*"` + `credentials: true`~~ |
| 3 | ✅ **ফিক্স হয়ে গেছে** — [client-side/src/auth/dbUrl.js](client-side/src/auth/dbUrl.js) এখন `process.env.REACT_APP_API_BASE_URL` দিয়ে | ~~Hardcoded `http://localhost:5053` API URL~~ |
| 4 | ✅ **ফিক্স হয়ে গেছে** — `authController.js` এখন পাসওয়ার্ড ছাড়া payload বানায় (কমেন্ট: "Generates a JWT token without including the password") | ~~JWT payload contains password hash~~ |
| 5 | ✅ **ফিক্স হয়ে গেছে** — `authMiddleware` + `requireRole` এখন ৮টা route file-এর প্রায় সব রুটে বসানো (শুধু `homeRouter.js` আর `/submit` public) | ~~No auth middleware on notice/answer/dataCheck/sumsData/user CRUD routes~~ |
| 6 | ✅ **ফিক্স হয়ে গেছে** — `middleware/roleMiddleware.js`-এর `requireRole(...)` প্রতিটা route file-এ role-check করে | ~~No RBAC (admin can hit branch endpoints, branch can hit zonal)~~ |
| 7 | ✅ **ফিক্স হয়ে গেছে** — `authMiddleware.js` এখন `authHeader.startsWith("Bearer ")` চেক করে, ক্লায়েন্টেও কোথাও `"myworld"` নেই | ~~Non-standard auth scheme `"myworld <token>"` instead of `Bearer`~~ |
| 8 | ⬜ **বাকি** — এখনো `server-side/package.json`-এ helmet/express-rate-limit নেই | No helmet, no rate-limit, no input sanitization |

### 🟠 Broken/Incomplete Code
| # | File | Issue |
|---|------|-------|
| 9 | ✅ **ফিক্স হয়ে গেছে** — `authController.js`-এর `logout` এখন শুধু `res.status(200).json({ message: "Logged out successfully" })` রিটার্ন করে | ~~Logout uses `window.localStorage` (server-side) + `res.render()`~~ |
| 10 | ✅ **ফিক্স হয়ে গেছে (রুট কারণ ঠিক করে)** — `answerController.js`-এর `update`-এ redundant `.save()` কলটাই সরিয়ে দেওয়া হয়েছে যা আগের আপডেট মুছে দিচ্ছিল (`.save()` await করার বদলে পুরোপুরি বাদ) | ~~`.save()` not awaited in update path~~ |
| 11 | ✅ **ফিক্স হয়ে গেছে** — [AdminReview.jsx](client-side/src/dashboard/users/database/AdminReview.jsx) এখন ২৩৫ লাইনের কাজ করা কম্পোনেন্ট, `/dashboard/notice-overview` রুটে ওয়্যার করা | ~~Empty component, wired to `/dashboard/admin-submission`~~ |
| 12 | ✅ **অপ্রাসঙ্গিক হয়ে গেছে** — পুরো layout রিফ্যাক্টর হয়ে গেছে; `BackendSideNav.jsx`/`BackendHeader.jsx` ফাইল দুটোই আর নেই (এখন `layouts/DashboardLayout.jsx` + `components/AppHeader.jsx`), নতুন হেডারে `#/` dead link পাওয়া যায়নি | ~~Sidebar links "Regional Reviews", "Branch Reviews", "Main Users" point to `#/`~~ |
| 13 | ✅ **অপ্রাসঙ্গিক হয়ে গেছে** — `Math.random()` দিয়ে Notice ID জেনারেশন কোথাও পাওয়া যায়নি (পুরনো `BackendHeader.jsx` ফাইলটাই আর নেই) | ~~Notice ID generated by `Math.random()` on client~~ |
| 14 | ⬜ **বাকি** — `adminModel.js`, `branchModel.js`, `zonalModel.js`, `checkModel.js` এখনো ফাইলে আছে, কোনো controller import করে না | `branchModel`, `zonalModel`, `adminModel`, `checkModel` defined but never used — all code uses `thanaModel` |

### 🟡 Cleanup
- ✅ `client-side/build.zip`, `server-side/clone-backend.zip`, `server-side/time.js` — সবগুলো আর রিপোতে নেই
- ✅ `server-side/branchDatabase.js`, `userDatabase.js`, `zonalDatabase.js` — সরানো হয়েছে, এখন `seeder.js` দিয়ে সিড করা হয় (দেখুন CLAUDE.md)
- ⬜ **বাকি (গুরুত্বপূর্ণ)** — `server-side/request.http` এখন gitignored, কিন্তু ignore করার আগে commit `1981fcc`-এ real JWT token পুশ হয়ে গেছে — token/secret rotate করা ও git history clean করা এখনো বাকি
- ⬜ **বাকি** — Typos: `Dadeline` → `Deadline` (২৬৫টা occurrence, ৪০ ফাইলে — যাচাই করা সংখ্যা)
- ✅ Unused deps `redux`, `react-redux`, `axios` — client-side/package.json থেকে আগেই সরানো হয়েছে
- ✅ `@mui/material` server-side থেকে সরানো হয়েছে (`npm uninstall`, package.json + package-lock.json + node_modules তিনটা থেকেই)

---

## Step-by-Step Execution Plan

### Phase 0: Backup & Branch (30 min)

```bash
cd /home/abwahab/Project_Running/project-form-suite
git add -A && git commit -m "snapshot: before production hardening"
git checkout -b production-hardening
```

**Verify:** `git status` clean, on new branch.

---

### Phase 1: Environment & Secrets (2-3 hours)

> ✅ **এই ফেজ হয়ে গেছে** — `.env` ফাইল, `process.env.JWT_SECRET`, `CORS_ORIGIN`, JWT payload থেকে password বাদ, `Bearer` scheme — সব যাচাই করা হয়েছে বর্তমান কোডে। শুধু বাকি: leaked token-এর জন্য secret **rotate** করা ও git history clean করা (দেখুন Known Issues / Cleanup সেকশন) — এটা কোড এডিট না, প্রোডাকশন অ্যাকশন।

**Goal:** No secret stays in code.

1. **Create `server-side/.env`** (gitignored):
   ```
   PORT=5053
   MONGODB_URI=mongodb://localhost:27017/instancereport
   JWT_SECRET=<generate with: openssl rand -hex 32>
   JWT_EXPIRES_IN=7d
   CORS_ORIGIN=http://localhost:3000
   NODE_ENV=development
   ```

2. **Create `server-side/.env.example`** (committed): same keys, empty values.

3. **Update [server-side/index.js](server-side/index.js):**
   - Line 12: `const port = process.env.PORT || 5053;`
   - Line 16: `origin: process.env.CORS_ORIGIN?.split(",") || false`
   - Add `require("dotenv").config();` at the very top.

4. **Update [server-side/controller/authController.js](server-side/controller/authController.js):**
   - Replace all 4 occurrences of `"3e9b2825-..."` with `process.env.JWT_SECRET`.
   - Remove password from JWT payload (lines 25, 56, 89, 112) — keep only `{userId, userRole, _id}`.
   - Use `process.env.JWT_EXPIRES_IN` for expiry.

5. **Update [server-side/middleware/authMiddleware.js](server-side/middleware/authMiddleware.js):**
   - Line 20: `jwt.verify(token, process.env.JWT_SECRET, ...)`.
   - Line 6: Change `"myworld "` → `"Bearer "` (and update client too — see step 7).
   - Remove `console.error` debug logs.

6. **Create `client-side/.env`** (gitignored):
   ```
   REACT_APP_API_BASE_URL=http://localhost:5053
   ```
   And `client-side/.env.production`:
   ```
   REACT_APP_API_BASE_URL=https://instancereportbackend.deepseahost.com
   ```

7. **Update [client-side/src/auth/dbUrl.js](client-side/src/auth/dbUrl.js):**
   ```js
   const BASE_URL = process.env.REACT_APP_API_BASE_URL;
   ```
   Then global find-replace: `"myworld "` → `"Bearer "` across all `.jsx` files (44 occurrences).

**Verify:** Restart backend and frontend; login still works; localStorage shows token; no hardcoded URL/secret on `grep -r "3e9b2825\|myworld \|localhost:5053" server-side client-side/src`.

---

### Phase 2: Auth Middleware on All Protected Routes (3-4 hours)

> ✅ **এই পুরো ফেজ হয়ে গেছে** — role middleware তৈরি হয়েছে (নাম `server-side/middleware/roleMiddleware.js`, নিচের প্ল্যানে বলা `requireRole.js` না, কিন্তু একই `requireRole(...roles)` এক্সপোর্ট করে), সব route file-এ বসানো হয়েছে, আর logout এন্ডপয়েন্ট ফিক্স হয়ে গেছে। নিচের ধাপগুলো রেফারেন্সের জন্য।

**Goal:** Every endpoint requires a valid JWT, except `/`, `/submit`.

1. **Create `server-side/middleware/roleMiddleware.js`:**
   ```js
   module.exports = (...allowedRoles) => (req, res, next) => {
     if (!allowedRoles.includes(req.userData?.userRole))
       return res.status(403).json({ error: "Forbidden" });
     next();
   };
   ```

2. **Apply `authMiddleware` to every route file:**
   - `noticeRoutes.js` — all CRUD requires auth
   - `answerRoutes.js` — all requires auth
   - `dataCheckRoutes.js` — all requires auth + `requireRole` (admin/zonal/branch/thana per path)
   - `sumsDataRoutes.js` — admin-only via `requireRole("admin")`
   - `usersCreateUpdateRoutes.js` — admin-only
   - `dashboardRoutes.js` — auth required; `/thana-users`, `/zonal-users`, `/branch-users` need role check

3. **Fix logout endpoint** [server-side/controller/authController.js](server-side/controller/authController.js):
   ```js
   exports.logout = (req, res) => res.json({ ok: true });
   ```
   (Client clears localStorage; no server-side state to clear unless using a token blocklist.)

**Verify:**
- `curl http://localhost:5053/all-notice` returns 401.
- With valid token: returns data.
- Branch user calling `/admin/data-checkout/...` returns 403.

---

### Phase 3: Hardening Middleware (1-2 hours)

1. `npm install helmet express-rate-limit --prefix server-side`
2. **Update [server-side/index.js](server-side/index.js):**
   ```js
   const helmet = require("helmet");
   const rateLimit = require("express-rate-limit");
   server.use(helmet());
   server.use("/submit", rateLimit({ windowMs: 15 * 60 * 1000, max: 10 }));
   server.use(rateLimit({ windowMs: 60 * 1000, max: 100 }));
   ```
3. Add a global error handler at the end:
   ```js
   server.use((err, req, res, next) => {
     console.error(err);
     res.status(err.status || 500).json({ error: err.message || "Internal error" });
   });
   ```

**Verify:** Login 11 times in 15 minutes — 11th call gets 429.

---

### Phase 4: Fix Broken Components (2-3 hours)

1. **AdminReview** — delete [client-side/src/dashboard/users/database/AdminReview.jsx](client-side/src/dashboard/users/database/AdminReview.jsx); remove its import + `<Route>` from `App.jsx`; remove the sidebar link.

2. **Sidebar dead links / About / Contact / Weather** — leave as-is per user decision; no work here this round.

3. **Notice ID generation** — move from client `Math.random()` to backend (let MongoDB generate `_id`, or use `crypto.randomUUID()` server-side).

4. **Fix `answerController.update`** [server-side/controller/answerController.js:64](server-side/controller/answerController.js#L64): `await response.save();` then return.

5. **Standardize error responses** across controllers: always `res.status(code).json({ error: msg })`.

**Verify:** Walk through each role's UI; no dead links; create/update/delete notice end-to-end works.

---

### Phase 5: Dependency Updates (2-4 hours)

**Goal:** নিরাপদ patch/minor আপডেট প্রথমে, major upgrades আলাদাভাবে (breaking changes এর জন্য)। Audit-এ ১৬টা server dep + ১৯টা client dep পুরনো পাওয়া গেছে।

**Step 1: Audit + safe patches (15 min)**
```bash
# দুই সাইডে security audit
cd server-side && npm audit
cd ../client-side && npm audit

# Safe patch + minor updates
cd ../server-side && npm update
cd ../client-side && npm update
```

**Step 2: Server-side major upgrades** (একটা একটা করে, প্রতিটায় smoke test):

| Package | From → To | Risk | Notes |
|---------|-----------|------|-------|
| `bcrypt` | 5 → 6 | Low | Node ≥18 required |
| `bcryptjs` | 2 → 3 | Low | API stable |
| `body-parser` | 1 → 2 | Low | Express 5 এর জন্য দরকার |
| `ejs` | 3 → 5 | Skip | EJS ব্যবহারই হয় না (logout এ broken `res.render` ছাড়া) |
| `express` | 4 → 5 | **HIGH** | Async error handling বদলেছে, deprecated APIs সরিয়েছে — Phase 2 শেষে করুন |
| `mongoose` | 8 → 9 | **HIGH** | Strict query, populate change — connection + every query verify |
| `multer` | 1 → 2 | Medium | API বদলেছে — file upload পথে testing দরকার |
| `mongodb` | 6 → 7 | Medium | Mongoose 9 এর সাথে compatible |
| `@mui/material` | ✅ server-side থেকে remove হয়ে গেছে | — | upgrade দরকার নেই |

**Step 3: Client-side major upgrades:**

| Package | From → To | Risk | Notes |
|---------|-----------|------|-------|
| `react` + `react-dom` | 18 → 19 | **HIGH** | React 19 stable, কিন্তু `react-scripts` 5 এখনও 19 অফিশিয়ালি সাপোর্ট করে না — **আপাতত 18-এ থাকুন** |
| `react-router-dom` | 6 → 7 | Medium | Mostly compatible; HashRouter import বদলায়নি |
| `react-redux` + `redux` | — | Skip | Phase 6 cleanup-এ remove হবে (unused) |
| `axios` | — | Skip | Phase 6 cleanup-এ remove হবে (unused) |
| `react-loader-spinner` | 6 → 8 | Low | Visual check |
| `sweetalert2` | 11.11 → 11.26 | Low | Patch-level only |
| `@mui/material` + icons | 5 → 9 | **HIGH** | MUI v6/v7/v8 breaking changes (theme, Grid, Stack) — অনেক UI ভাঙতে পারে। **আপাতত v5 এ থাকুন**, post-launch upgrade |

**Recommendation for production push:**
- ✅ **এখন করুন:** `npm audit fix`, safe minor/patch updates, bcrypt/bcryptjs/body-parser/multer upgrade
- 🟡 **শুধু সময় থাকলে:** express 5, mongoose 9, react-router-dom 7
- ❌ **পরে করুন (post-launch):** React 19, MUI v9 (UI regression risk বড়)

**Verify:** প্রতিটা major upgrade-এর পর Phase 7 smoke test চালান।

---

### Phase 6: Cleanup (1 hour)

> ✅ আর্টিফ্যাক্ট (build.zip, clone-backend.zip, time.js) মুছে ফেলা, root seed files সরানো, redux/react-redux/axios বাদ দেওয়া, আর `@mui/material` server-side থেকে বাদ দেওয়া — সব হয়ে গেছে। নিচের bash ব্লকটা রেফারেন্সের জন্য রাখা হলো, নতুন করে চালানোর দরকার নেই।

```bash
# Remove committed artifacts
git rm client-side/build.zip server-side/clone-backend.zip
git rm server-side/time.js server-side/request.http

# Move dev seed files
mkdir -p server-side/model/seeders/dev
git mv server-side/branchDatabase.js server-side/model/seeders/dev/
git mv server-side/userDatabase.js server-side/model/seeders/dev/
git mv server-side/zonalDatabase.js server-side/model/seeders/dev/

# Rotate the seeded passwords from "1122" to a secure default
```

- ✅ ~~Drop unused deps: `cd client-side && npm uninstall redux react-redux axios`~~ — done
- ✅ ~~Drop `@mui/material` from server-side package.json~~ — done (`npm uninstall`)
- ⬜ **বাকি** — Delete unused models: `adminModel.js`, `branchModel.js`, `zonalModel.js`, `checkModel.js` (controllers only use `thanaModel`)
- ⬜ **বাকি** — Find-replace `Dadeline` → `Deadline` and `assects` → `assets` repo-wide

**Verify:** `npm install` clean both sides; `npm run build` succeeds in client-side.

---

### Phase 7: Production Build Config (2 hours)

1. **Backend Dockerfile** (`server-side/Dockerfile`):
   ```dockerfile
   FROM node:20-alpine
   WORKDIR /app
   COPY package*.json ./
   RUN npm ci --omit=dev
   COPY . .
   EXPOSE 5053
   CMD ["node", "index.js"]
   ```

2. **Frontend Dockerfile** (`client-side/Dockerfile`) — multi-stage with nginx:
   ```dockerfile
   FROM node:20-alpine AS build
   WORKDIR /app
   COPY package*.json ./
   RUN npm ci
   COPY . .
   RUN npm run build

   FROM nginx:alpine
   COPY --from=build /app/build /usr/share/nginx/html
   COPY nginx.conf /etc/nginx/conf.d/default.conf
   ```
   And `client-side/nginx.conf` with SPA fallback (`try_files $uri /index.html`).

3. **Update root `docker-compose.yml`** to include `server` and `client` services next to `mongodb`, with env_file references.

4. **Fix `.htaccess`** — remove `RewriteBase /subdirectory` if deploying at root; or make it match real subpath.

**Verify:** `docker compose up --build` → can hit `http://localhost/`, login works, end-to-end notice creation works.

---

### Phase 8: Smoke Test Checklist (1-2 hours)

Run through each role logged in (use the new seeder credentials):

**Admin:**
- [ ] Login → dashboard loads
- [ ] Create notice with multiple questions
- [ ] View all branch/zonal/thana users
- [ ] Create + edit + delete a branch user
- [ ] Open admin-data-interface for an existing notice
- [ ] sumsData reports render with correct totals
- [ ] Logout

**Zonal:**
- [ ] Login → only own zone users visible
- [ ] Can view sums for own zone, blocked from other zones (403)
- [ ] Cannot create users (admin-only)

**Branch:**
- [ ] Login → branch dashboard
- [ ] View notices, answer one, edit own answer
- [ ] Cannot see other branches' data

**Thana:**
- [ ] Login → can answer assigned notices
- [ ] Cannot access admin pages

Capture any bug → re-open the relevant phase.

---

### Phase 9: VPS Deploy via Docker (2-4 hours)

1. **VPS prep** (Ubuntu/Debian assumed):
   ```bash
   apt update && apt install -y docker.io docker-compose-plugin git
   ufw allow 22 && ufw allow 80 && ufw allow 443 && ufw enable
   ```
2. **Clone repo** to `/opt/project-form-suite`; create `.env` files on the server (don't commit prod secrets).
3. **Add Caddy as reverse proxy** in `docker-compose.yml`:
   ```yaml
   caddy:
     image: caddy:2-alpine
     ports: ["80:80", "443:443"]
     volumes:
       - ./Caddyfile:/etc/caddy/Caddyfile
       - caddy_data:/data
   ```
   `Caddyfile`:
   ```
   instancereport.deepseahost.com {
     reverse_proxy client:80
   }
   instancereportbackend.deepseahost.com {
     reverse_proxy server:5053
   }
   ```
   Caddy handles Let's Encrypt automatically.
4. **Enable MongoDB auth** — add `MONGO_INITDB_ROOT_USERNAME/PASSWORD` to `mongodb` service; update `MONGODB_URI` to include creds.
5. **Point DNS** for both domains at VPS IP.
6. **First boot:**
   ```bash
   docker compose up -d --build
   docker compose exec server node seeder.js  # one-time
   ```
7. **Verify** logs: `docker compose logs -f server client caddy`.

**Verify:** Login from production URL works, full smoke test (Phase 7) runs green on live.

---

## Estimated Total Time

| Phase | Time |
|-------|------|
| 0. Backup & branch | 30 min |
| 1. Env & secrets | 2-3 hr |
| 2. Auth on all routes | 3-4 hr |
| 3. Hardening middleware | 1-2 hr |
| 4. Fix broken components | 2-3 hr |
| 5. Dependency updates | 2-4 hr |
| 6. Cleanup | 1 hr |
| 7. Build config | 2 hr |
| 8. Smoke test | 1-2 hr |
| 9. Deploy | 2-4 hr |
| **Total** | **~17-26 hours (3 working days)** |

---

## Critical Files to Touch

**Backend:**
- [server-side/index.js](server-side/index.js)
- [server-side/middleware/authMiddleware.js](server-side/middleware/authMiddleware.js)
- [server-side/middleware/roleMiddleware.js](server-side/middleware/roleMiddleware.js) (already added)
- [server-side/controller/authController.js](server-side/controller/authController.js)
- [server-side/controller/answerController.js](server-side/controller/answerController.js)
- All files in [server-side/router/routes/](server-side/router/routes/)
- [server-side/.env](server-side/.env) (new), [server-side/.env.example](server-side/.env.example) (new)

**Frontend:**
- [client-side/src/auth/dbUrl.js](client-side/src/auth/dbUrl.js)
- [client-side/src/contexts/AuthContext.jsx](client-side/src/contexts/AuthContext.jsx) (header change)
- ~~`client-side/src/dashboard/layouts/BackendSideNav.jsx`~~ / ~~`BackendHeader.jsx`~~ — both files no longer exist; layout was rebuilt as [client-side/src/layouts/DashboardLayout.jsx](client-side/src/layouts/DashboardLayout.jsx) + [client-side/src/components/AppHeader.jsx](client-side/src/components/AppHeader.jsx)
- [client-side/src/dashboard/users/database/AdminReview.jsx](client-side/src/dashboard/users/database/AdminReview.jsx) — no longer empty, see Decisions Locked below
- [client-side/.env](client-side/.env) (new), [client-side/.env.production](client-side/.env.production) (new)

**Root:**
- [docker-compose.yml](docker-compose.yml)
- [.htaccess](.htaccess)
- [.gitignore](.gitignore) (already good)

---

## Decisions Locked

| Question | Decision |
|----------|----------|
| Deployment target | **VPS + Docker** — full stack via docker-compose (mongo + server + client behind Caddy/Nginx reverse proxy with auto-TLS) |
| AdminReview page | ~~Remove~~ — **superseded**: instead of deleting it, the component was actually built out (235 lines, wired to `/dashboard/notice-overview`) |
| Sidebar dead links / About / Contact / Weather | **Moot** — the old sidebar (`BackendSideNav.jsx`) doesn't exist anymore; layout was rebuilt from scratch |
| Token scheme | ✅ **Done** — `Bearer <token>` in place of `"myworld "`, both `authMiddleware.js` and all client call sites |
| Production domain | Assumed `instancereport.deepseahost.com` (commented in code) — **verify before Phase 1** |
| MongoDB host | Docker MongoDB on same VPS via docker-compose (with auth enabled) |
